import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reklam Merkezi · Rast OS" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
