import type { Metadata } from "next";

export const metadata: Metadata = { title: "İçerik Takvimi · Rast OS" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
